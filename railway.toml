[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c 'exec gunicorn libros.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2'"
releaseCommand = "python manage.py migrate --noinput"
healthcheckPath = "/autores/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
